import { ShowCitiesBtn } from "../Buttons/ShowCitiesBtn";

import "../Header/Header.css";

export const Header = () => {
  return (
    <header className="app-header">
      <h1>Weather App</h1>
    </header>
  );
};
